Backend for the Tax Regime Website

•	Run the ‘npm install’ command inside the backend folder in order to install the packages
•	Inside the backend folder, run ‘npm run dev’ command to activate the server.

