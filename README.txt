•	Run the ‘npm install’ command inside both the frontend and the backend folders in order to install the packages
•	Inside the backend folder, run ‘npm run dev’ command to activate the server.
•	Inside the frontend folder, run ‘npm start’ command to load the UI.
